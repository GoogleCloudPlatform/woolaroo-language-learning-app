version: '3'

services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile-dev
    ports:
      - "4200:4200"
    tty: true
    volumes:
      - ./app:/var/www/app
      - /var/www/app/node_modules/